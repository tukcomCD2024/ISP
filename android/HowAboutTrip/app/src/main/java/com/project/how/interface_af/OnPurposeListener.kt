package com.project.how.interface_af

interface OnPurposeListener {
    fun onWhoListener(who : String)
    fun onActivityLevelListener(activityLevel : String)
    fun onThemeListener(theme : List<String>)
}